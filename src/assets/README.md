# assets/

Build-processed static assets (images, fonts, etc.).

## Usage

Unlike `public/`, files in `src/assets/` are processed by Vite's build pipeline:
- Hashed filenames for cache busting
- Optimized and bundled

```jsx
import logo from './assets/logo.png';
```

## Current Contents

This directory is currently empty. Add images, SVGs, or other assets that need build processing here.
