# AGENTS.md — Context Tests
AI Agent Context for the Context tests.
Provides test coverage for the application's global React contexts (`AppContext` and `ActivityBankContext`), driving behavior with real methods.
