# AGENTS.md — Data Tests

AI Agent Context for the data structure tests.
Validates the `modules.js` registry: schema keys, all 10 modules available, metadata for `ModulePage.jsx`, **`quiz`** shape (three questions per module), and **`glossary`** key lists.
